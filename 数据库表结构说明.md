# 数据库表结构说明

## 项目概述

### 数据库架构
- **数据库类型**: PostgreSQL
- **连接地址**: postgresql://postgres:123@localhost:5432/postgres
- **ORM框架**: Sequelize
- **表数量**: 3个核心业务表

---

## 数据库表结构详情

### 1. users 表 - 用户管理表
**功能**: 存储系统用户信息和认证数据

**表结构**:
```sql
CREATE TABLE users (
    id BIGINT PRIMARY KEY AUTO_INCREMENT,
    username VARCHAR(50) UNIQUE NOT NULL,
    email VARCHAR(100) UNIQUE NOT NULL,
    password_hash VARCHAR(100) NOT NULL,
    full_name VARCHAR(100),
    phone_number VARCHAR(20),
    status VARCHAR(20) NOT NULL,
    email_verified BOOLEAN DEFAULT false,
    phone_verified BOOLEAN DEFAULT false,
    last_login_at TIMESTAMP,
    failed_login_attempts INTEGER DEFAULT 0,
    locked_until TIMESTAMP,
    created_at TIMESTAMP DEFAULT NOW(),
    updated_at TIMESTAMP DEFAULT NOW()
);
```

**核心功能**:
- ✅ 用户注册和认证
- ✅ 密码加密存储（bcrypt）
- ✅ 登录失败次数限制
- ✅ 账户锁定机制
- ✅ 邮箱和手机验证状态跟踪

**关联关系**:
- 被 `documents` 表的外键 `uploadedBy` 引用

---

### 2. documents 表 - 文档管理表
**功能**: 存储上传的文档信息和处理状态

**表结构**:
```sql
CREATE TABLE documents (
    id INTEGER PRIMARY KEY AUTO_INCREMENT,
    title VARCHAR(255) NOT NULL,
    filename VARCHAR(255) NOT NULL,
    originalName VARCHAR(255) NOT NULL,
    fileType ENUM('pdf', 'docx', 'doc', 'txt') NOT NULL,
    fileSize INTEGER NOT NULL,
    filePath VARCHAR(255) NOT NULL,
    content TEXT DEFAULT '',
    wordCount INTEGER DEFAULT 0,
    characterCount INTEGER DEFAULT 0,
    chunkCount INTEGER DEFAULT 0,
    processingStatus ENUM('pending', 'processing', 'completed', 'failed') DEFAULT 'pending',
    processingError TEXT DEFAULT '',
    isIndexed BOOLEAN DEFAULT false,
    metadata JSONB DEFAULT '{}',
    uploadedBy INTEGER NOT NULL,
    createdAt TIMESTAMP NOT NULL,
    updatedAt TIMESTAMP NOT NULL,
    FOREIGN KEY (uploadedBy) REFERENCES users(id) ON UPDATE CASCADE ON DELETE CASCADE
);
```

**核心功能**:
- ✅ 多格式文档存储（PDF、DOCX、DOC、TXT）
- ✅ 文档处理状态跟踪
- ✅ 向量化索引状态管理
- ✅ 文档内容提取和统计
- ✅ 元数据存储（JSON格式）

**索引优化**:
- 主键索引
- 处理状态索引
- 索引状态索引
- 用户-创建时间联合索引

---

### 3. userbackup 表 - 用户备份表
**功能**: 用户数据的备份表（迁移或测试用途）

**表结构**:
```sql
CREATE TABLE userbackup (
    id BIGINT PRIMARY KEY AUTO_INCREMENT,
    username VARCHAR(50) UNIQUE NOT NULL,
    email VARCHAR(100) UNIQUE NOT NULL,
    password_hash VARCHAR(100) NOT NULL,
    full_name VARCHAR(100),
    phone_number VARCHAR(20),
    status VARCHAR(20) NOT NULL,
    email_verified BOOLEAN DEFAULT false,
    phone_verified BOOLEAN DEFAULT false,
    last_login_at TIMESTAMP,
    failed_login_attempts INTEGER DEFAULT 0,
    locked_until TIMESTAMP,
    created_at TIMESTAMP DEFAULT NOW(),
    updated_at TIMESTAMP DEFAULT NOW()
);
```

**特点**:
- 与 `users` 表结构类似
- 使用 `bigint` 作为主键类型
- 包含额外的索引优化

---

## 数据库设计亮点

### 1. 安全性设计
- **密码加密**: 使用 bcrypt 算法加密存储
- **账户安全**: 登录失败次数限制和账户锁定机制
- **数据验证**: 邮箱格式验证、唯一性约束

### 2. 性能优化
- **索引策略**: 针对查询频率高的字段建立索引
- **外键约束**: 确保数据完整性和关联性
- **JSONB存储**: 灵活存储文档元数据

### 3. 业务逻辑支持
- **状态管理**: 完整的文档处理状态跟踪
- **统计功能**: 支持文档数量、字数等统计
- **关联关系**: 用户与文档的完整关联体系

---

## 系统集成关系

### 与后端服务集成
- **认证服务**: 基于 users 表的用户认证
- **文档服务**: 基于 documents 表的文档管理
- **向量搜索**: 与 Pinecone 向量数据库协同工作

### API 接口支持
- 用户注册/登录接口
- 文档上传/查询接口
- 文档处理状态查询接口
- 统计信息查询接口

---

## 技术实现价值

### 业务价值
- **用户管理**: 完整的用户生命周期管理
- **文档管理**: 企业级文档处理流水线
- **数据安全**: 企业级安全标准和数据保护
- **扩展性**: 支持业务功能持续扩展

### 技术价值
- **架构设计**: 清晰的数据层分离
- **性能优化**: 针对性的索引和查询优化
- **可维护性**: 标准化的数据库设计和文档

---

## 未来扩展方向

### 功能扩展
- 文档版本管理
- 权限分级控制
- 操作日志记录
- 数据备份恢复

### 性能优化
- 分区表设计
- 读写分离
- 缓存策略优化
- 查询性能监控

---

*本数据库设计为金融文档问答系统提供了稳定、安全、高效的数据存储基础，支持系统的核心业务功能正常运行。*